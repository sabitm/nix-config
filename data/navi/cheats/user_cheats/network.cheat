% networking

# fast scan active host on a network without port
sudo nmap -n -T4 -sn <network_ip>/<netmask>

# fast scan active host on a network with port
sudo nmap -n -T4 -F <network_ip>/<netmask>

# list processes associated on specific IPv4 port
lsof -i4

$ network_ip: ip r | grep '^1' | cut -d' ' -f1
